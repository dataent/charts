import * as Charts from './chart';

let dataent     = { };

dataent.NAME    = 'Dataent Charts';
dataent.VERSION = '1.2.0';

dataent         = Object.assign({ }, dataent, Charts);

export default dataent;